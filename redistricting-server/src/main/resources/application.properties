spring.application.name=redistricting-server
spring.mongodb.uri=mongodb://localhost:27017/redistricting

# Path to preprocessing JSON output directory (absolute or relative to working directory).
# Used by JsonDataSeeder when running with --spring.profiles.active=seed
seeder.json.path=../preprocessing/Output
